# Adapter note — Codex

- `gh` and the network are unreachable from your sandbox. The issue bodies in
  `contract/` were materialized for you and are the binding contract — do not
  substitute acceptance criteria inferred from specs or code.
- You will not commit, and that is fine — the conductor commits and owns the
  per-issue split. Do not spend effort on git; spend it on the code and on your
  final message's per-issue file map.
